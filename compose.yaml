services:
  z-image-mcp:
    build: .
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      - ENABLE_CPU_OFFLOAD=true
      - DEFAULT_NUM_IMAGES=1  # or any other number
    volumes:
      - ${HOME}/.cache/huggingface:/root/.cache/huggingface:rw
